import React from 'react'
import ReactDOM from 'react-dom/client'
import Session from './Session.jsx'

ReactDOM.createRoot(document.getElementById('session')).render(
  <React.StrictMode>
    <Session />
  </React.StrictMode>

)
